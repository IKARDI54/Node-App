La app se aarancaría con: npm run dev
La db para que arranque en otra terminal: mongod
Para consultar la db en otra terminal: 
    -mongo (arrancar)
    -use notes-db-app (use <name db>)
    -db.notes.find().pretty() (verías el contenido, siendo notes el nombre de la collection de la d.b)